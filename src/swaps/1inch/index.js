async function getSupportedPairs () {
  return []
}

async function getQuote ({ commit, getters, state }, { network, from, to, amount }) {
  return null
}

export default {
  getSupportedPairs,
  getQuote
}
